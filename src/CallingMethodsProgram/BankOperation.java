package CallingMethodsProgram;

public class BankOperation {

	// verify card number and pin
	// verify card info using boolean
	// deposit money in the account

	String accountName = "Thomas";
	double accountBalance = 0;
	String cardNumber = "5211521152115211";
	int pinNumber = 1989;

	double depositMoney(double amountToDeposit, double pinNumber) {

		if (cardNumber.equals(cardNumber) && (pinNumber == pinNumber)) {

			return accountBalance;

		}

		return 0;
	}

}
